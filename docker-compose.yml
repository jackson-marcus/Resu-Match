services:
  api:
    build: .
    env_file: .env
    ports:
      - "8130:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      RESUMATCH_API_URL: http://api:8000
    ports:
      - "8631:8501"
    depends_on:
      - api
